#############################
# Provider specific Inputs
#############################

# The public IP of the manager to which the CLI will connect.
vm_ip: '192.168.122.73'

# SSH user used to connect to the manager
ssh_user: 'centos'

#password to connect to 73
ssh_password: centos
